DATASET_YELP_PATH=/absolute/path/to/yelp_reviews.jsonl
DATASET_AMAZON_PATH=/absolute/path/to/amazon_reviews.jsonl
DATASET_GOODREADS_PATH=/absolute/path/to/goodreads_reviews.jsonl
DETERMINISTIC_MODE=false
PROFILE_CACHE_TTL_SECONDS=3600
PROFILE_CACHE_MAX_SIZE=1000
ENABLE_LLM=false
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o
# Optional: path to a pre-built vector store JSON produced by the ingestion CLI.
# When set and the file exists, the API loads it at startup instead of an empty store.
VECTOR_STORE_PATH=
